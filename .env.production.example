POSTGRES_PASSWORD=CHANGE_TO_A_LONG_RANDOM_PASSWORD
NODE_ENV=production
PORT=3000
# DATABASE_URL is supplied by docker-compose.
# Generate a strong application/session secret if you add DB-backed sessions later.
