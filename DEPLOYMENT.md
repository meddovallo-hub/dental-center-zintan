# تجهيز V6 للإطلاق

1. أنشئ PostgreSQL database.
2. نفّذ `database.sql`.
3. انسخ `.env.example` إلى `.env` وضع بيانات قاعدة البيانات وكلمة مرور قوية.
4. نفّذ `npm install`.
5. شغّل `npm start`.
6. ضع HTTPS أمام التطبيق عبر reverse proxy.
7. فعّل نسخًا احتياطية يومية عبر `backup.sh`.
8. لا تستخدم بيانات الدخول الافتراضية.
9. خزّن الأسرار خارج Git.
10. اختبر: تسجيل الدخول، إنشاء الحجز، تعارض الموعد، تغيير الحالة، النسخ الاحتياطي، واستعادة نسخة.

ملاحظة: `server-dev.js` هو خادم النسخة التطويرية السابقة، بينما `server.js` هو هيكل الإنتاج المعتمد على PostgreSQL.
